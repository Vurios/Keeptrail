# React Native's C++ codegen writes very deep generated paths. Under pnpm's
# default isolated layout those land inside node_modules/.pnpm/<long-hash>/...
# and exceed the Windows 260-character path limit, failing the Android build.
# A hoisted layout keeps module paths short enough to build on Windows.
node-linker=hoisted
